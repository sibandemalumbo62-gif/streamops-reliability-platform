apiVersion: apps/v1
kind: Deployment
metadata:
  name: integrity-service
  namespace: streamops
  labels:
    app: integrity-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: integrity-service
  template:
    metadata:
      labels:
        app: integrity-service
    spec:
      containers:
      - name: integrity-service
        image: streamops/integrity-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8006
        env:
        - name: DATABASE_URL
          value: "postgresql://streamops:streamops_password@postgres-integrity:5432/integrity_db"
        - name: REDIS_URL
          value: "redis://redis:6379/2"
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: streamops-config
              key: KAFKA_BOOTSTRAP_SERVERS
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: integrity-service
  namespace: streamops
spec:
  selector:
    app: integrity-service
  ports:
  - port: 8006
    targetPort: 8006
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: integrity-service-hpa
  namespace: streamops
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: integrity-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
